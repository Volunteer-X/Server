scalar ObjectID
scalar Longitude
scalar Latitude
scalar URL
scalar DateTime

type Ping {
  id: ID!
  title: String!
  userID: ID!
  longitude: Longitude!
  latitude: Latitude!
  picks: [String!]!
  description: String
  url: URL
  radius: Float
  createdAt: DateTime
  media: [Media]
}

input CreatePingInput {
  userID: ID!
  title: String!
  picks: [String!]!
  longitude: Longitude!
  latitude: Latitude!
  description: String
  url: URL
  radius: Float
}

type UpdatePingInput {
  id: ID!
  title: String
  picks: [String!]
  longitude: Longitude
  latitude: Latitude
  description: String
  url: URL
  radius: Float
  media: [Media]
}

# type Query {
# }

type Media {
  Key: String!
  type: String!
}

type Mutation {
  createPing(createPingInput: CreatePingInput!): ID!
  updatePing(updatePingInput: UpdatePingInput!): ID!
}
